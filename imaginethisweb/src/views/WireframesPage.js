import React, {Component} from "react";
import Navigation from "../components/Navigation";

export class WireframesPage extends Component{
    render() {
        return(
            <div>
                <Navigation/>
                This is the page to display wireframes
            </div>
        )
    }
}